name: RevokeSecurityGroupRules
product: vm
use: revoke-security-group-rules
short: Removes security group rules
long: |-
  Remove security group rules.
  **Note**

  * Status of security group should be `Available`.
  * You can use [`DescribeSecurityGroups`](describesecuritygroups.md) to query the ID of the security group.
  * Editing is not supported for default security group.
examples:
- cmd: zeno vm revoke-security-group-rules --security-group-id your-security-group-id --rule-infos "{direction=egress, policy=accept, priority=6, ipProtocol=tcp, portRange=22/22, cidrIp=0.0.0.0/0}"
  desc: Remove security group rules.
sdk:
  service: vm
  version: '2026-04-01'
  action: RevokeSecurityGroupRules
parameters:
- name: security-group-id
  type: string
  required: true
  sdk-field: securityGroupId
  description: Security group ID.
- name: rule-infos
  type: object-array
  required: true
  sdk-field: ruleInfos
  description: Security group rules.
  array-style: json
  item-schema:
  - name: direction
    type: string
    description: |-
      Traffic directions.
      ingress: inbound rules.
      egress: outbound rules.
  - name: policy
    type: string
    description: |-
      Access strategy.
      Available values:
      accept: allows access by default.
      Only accept strategy is supported for now.
  - name: priority
    type: integer
    description: Rule priority.
  - name: ipProtocol
    type: string
    description: |-
      Transport protocol. The value is case sensitive.
      Available value:tcp: TCP protocol.udp: UDP protocol.icmp: ICMP protocol.all: all protocols supported.
  - name: portRange
    type: string
    description: |-
      Destination port range.
      Available values:For TCP and UDP protocols: The value ranges from 1 to 65535. Use a slash (/) to separate the start port number and the end port number. Examples: 1/200; incorrect example: 200/1.For ICMP protocol: -1/-1.For all protocols: -1/-1.
  - name: cidrIp
    type: string
    description: |-
      Source IP address range.
      Default value: 0.0.XX.XX/0.
  - name: description
    type: string
    description: Rule description.
