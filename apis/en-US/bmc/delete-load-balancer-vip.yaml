name: DeleteLoadBalancerVIP
product: bmc
use: delete-load-balancer-vip
short: Deletes a load balancing virtual IP (VIP)
long: Delete a load balancing virtual IP (VIP).
examples:
- cmd: zeno bmc delete-load-balancer-vip --vip-id xxxx
  desc: Delete the load balancer VIP.
sdk:
  service: bmc
  version: '2026-02-01'
  action: DeleteLoadBalancerVIP
parameters:
- name: vip-id
  type: string
  required: true
  sdk-field: vipId
  description: The ID of the load balancer VIP.
