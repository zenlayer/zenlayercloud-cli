name: DeleteListener
product: bmc
use: delete-listener
short: Deletes a load balance listener
long: Delete a load balance listener.
examples:
- cmd: zeno bmc delete-listener --listener-id xxxx
  desc: Delete a load balancer listener.
sdk:
  service: bmc
  version: '2026-02-01'
  action: DeleteListener
parameters:
- name: listener-id
  type: string
  required: true
  sdk-field: listenerId
  description: The listener ID.
