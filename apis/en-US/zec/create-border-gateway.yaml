name: CreateBorderGateway
product: zec
use: create-border-gateway
short: Creates a border gateway
long: Create a border gateway.
examples:
- cmd: zeno zec create-border-gateway --region-id asia-east-1 --vpc-id <vpcId> --label xxx --asn 65000 --advertised-subnet PART --advertised-subnet-ids <subnetId1> --advertised-subnet-ids <subnetId2>
  desc: Create a border gateway with region-level routing.
sdk:
  service: zec
  version: '2025-09-01'
  action: CreateBorderGateway
parameters:
- name: region-id
  type: string
  required: true
  sdk-field: regionId
  description: Region ID.
- name: vpc-id
  type: string
  required: true
  sdk-field: vpcId
  description: VPC ID.
- name: label
  type: string
  required: true
  sdk-field: label
  description: |-
    Border gateway name.
    This parameter must start and end with a number or a letter, containing 2 to 63 characters. Only letters, numbers, - and periods (.) are supported.
- name: asn
  type: integer
  required: true
  sdk-field: asn
  description: |-
    Autonomous System Number.
    The range is from 65000 to 65534 .
- name: routing-mode
  type: enum
  required: false
  sdk-field: routingMode
  description: Information on dynamic routing modes.
  enum-values:
  - value: Regional
    desc: Regional routing. Learn routes only within the region it was created in.
  - value: Global
    desc: Global routing. Dynamically learn and distribute routes across all regions using a single VPN or Interconnect.
- name: advertised-subnet
  type: enum
  required: false
  sdk-field: advertisedSubnet
  description: |-
    Subnet route advertisement.
    Default value: Advertise All .
  enum-values:
  - value: ALL
    desc: Advertise all.
  - value: NONE
    desc: Do not advertise.
  - value: PART
    desc: Advertise partial.
- name: advertised-subnet-ids
  type: string-array
  required: false
  sdk-field: advertisedSubnetIds
  description: |-
    Set of subnet IDs.
    For this field to take effect, advertisedSubnet must be set to PART.
- name: advertised-cidrs
  type: string-array
  required: false
  sdk-field: advertisedCidrs
  description: Custom IPv4 CIDR block list.
- name: advertised-route-ids
  type: string-array
  required: false
  sdk-field: advertisedRouteIds
  description: ID list of the custom routes to be advertised.
response:
- name: zbgId
  type: string
  description: Border gateway ID.
