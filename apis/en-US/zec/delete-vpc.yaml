name: DeleteVpc
product: zec
use: delete-vpc
short: Deletes a global VPC.
long: delete a global VPC.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteVpc
parameters:
- name: vpc-id
  type: string
  required: true
  sdk-field: vpcId
  description: Global VPC ID.
