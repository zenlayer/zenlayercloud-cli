name: DeleteRoute
product: zec
use: delete-route
short: Deletes a route.
long: delete a route.
examples:
- cmd: zeno zec delete-route --vpc-id <vpcId> --route-id <routeId>
  desc: Delete a route.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteRoute
parameters:
- name: route-id
  type: string
  required: true
  sdk-field: routeId
  description: Route ID.
