name: DescribeVncUrl
product: zec
use: describe-vnc-url
short: Obtains the VNC address of the instance.
long: obtain the VNC address of the instance.
examples:
- cmd: zeno zec describe-vnc-url --instance-id instanceId1
  desc: Obtain the VNC address of the instance.
sdk:
  service: zec
  version: '2025-09-01'
  action: DescribeVncUrl
parameters:
- name: instance-id
  type: string
  required: true
  sdk-field: instanceId
  description: ID of the instance.
