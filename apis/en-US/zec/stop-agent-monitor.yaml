name: StopAgentMonitor
product: zec
use: stop-agent-monitor
short: Disables QEMU Guest Agent (QGA) data collection
long: Disable QEMU Guest Agent (QGA) data collection.
examples:
- cmd: zeno zec stop-agent-monitor --instance-id instanceId1
  desc: Disable QEMU Guest Agent (QGA) data collection.
sdk:
  service: zec
  version: '2025-09-01'
  action: StopAgentMonitor
parameters:
- name: instance-id
  type: string
  required: true
  sdk-field: instanceId
  description: ID of the instance.
