name: DeleteCrossRegionBandwidth
product: zec
use: delete-cross-region-bandwidth
short: Deletes a cross-region bandwidth.
long: delete a private cross-region bandwidth.
examples:
- cmd: zeno zec delete-cross-region-bandwidth --cross-region-bandwidth-id "<your cross id>"
  desc: Delete a cross-region bandwidth.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteCrossRegionBandwidth
parameters:
- name: cross-region-bandwidth-id
  type: string
  required: true
  sdk-field: crossRegionBandwidthId
  description: Cross-region bandwidth ID.
