name: DescribeAvailableNats
product: zec
use: describe-available-nats
short: Obtains list of NAT gateways that can be bound to a border gateway
long: Obtain list of NAT gateways that can be bound to a border gateway.
examples:
- cmd: zeno zec describe-available-nats --zbg-id xxx
  desc: Obtain list of NAT gateways that can be bound to a border gateway.
sdk:
  service: zec
  version: '2025-09-01'
  action: DescribeAvailableNats
parameters:
- name: zbg-id
  type: string
  required: true
  sdk-field: zbgId
  description: Border gateway ID.
