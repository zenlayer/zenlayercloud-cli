name: RenewIpv6Cidr
product: zec
use: renew-ipv6-cidr
short: Restores an IPv6 CIDR block.
long: restore an IPv6 CIDR block.
examples:
- cmd: zeno zec renew-ipv6-cidr --cidr-id <cidrId>
  desc: Restore an IPv6 CIDR block.
sdk:
  service: zec
  version: '2025-09-01'
  action: RenewIpv6Cidr
parameters:
- name: cidr-id
  type: string
  required: true
  sdk-field: cidrId
  description: IPv6 CIDR block ID.
