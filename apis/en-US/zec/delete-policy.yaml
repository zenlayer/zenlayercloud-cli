name: DeletePolicy
product: zec
use: delete-policy
short: Deletes a protection strategy
long: Delete a protection strategy.
examples:
- cmd: zeno zec delete-policy --policy-id <policyId>
  desc: Delete a protection strategy.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeletePolicy
parameters:
- name: policy-id
  type: string
  required: true
  sdk-field: policyId
  description: Protection strategy ID.
