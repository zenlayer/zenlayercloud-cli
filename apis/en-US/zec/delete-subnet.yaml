name: DeleteSubnet
product: zec
use: delete-subnet
short: Deletes a subnet under a global VPC.
long: delete a subnet under a global VPC.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteSubnet
parameters:
- name: subnet-id
  type: string
  required: true
  sdk-field: subnetId
  description: Subnet ID.
