name: DeleteSubnet
product: zec
use: delete-subnet
short: Deletes a subnet under a global VPC
long: |-
  Delete a subnet under a global VPC.
  **Note**

  * Before deleting the subnet, clear all its resources, including vNICs, instances, NAT gateways, HaVips, and others.
  * Operations on default subnets are not supported.
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteSubnet
parameters:
- name: subnet-id
  type: string
  required: true
  sdk-field: subnetId
  description: Subnet ID.
