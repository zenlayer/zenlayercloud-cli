name: ConfigEipEgressIp
product: zec
use: config-eip-egress-ip
short: Sets the egress elastic IP.
long: set the egress elastic IP.
sdk:
  service: zec
  version: '2025-09-01'
  action: ConfigEipEgressIp
parameters:
- name: eip-id
  type: string
  required: true
  sdk-field: eipId
  description: ID of elastic IP.
