name: DescribeInstanceInternetStatus
product: bmc
use: describe-instance-internet-status
short: 查询实例带宽、流量包状态
long: 查询实例带宽、流量包状态。
sdk:
  service: bmc
  version: '2026-02-01'
  action: DescribeInstanceInternetStatus
parameters:
- name: instance-id
  type: string
  required: true
  sdk-field: instanceId
  description: |-
    待操作的实例ID。
    可通过DescribeInstances接口返回值中的instanceId获取。
