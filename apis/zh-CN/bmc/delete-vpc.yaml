name: DeleteVpc
product: bmc
use: delete-vpc
short: 删除VPC
long: 本接口用于删除一个Vpc。
examples:
- cmd: zeno bmc delete-vpc --vpc-id your-vpc-id
  desc: 删除VPC。
sdk:
  service: bmc
  version: '2026-02-01'
  action: DeleteVpc
parameters:
- name: vpc-id
  type: string
  required: true
  sdk-field: vpcId
  description: VPC的ID。
