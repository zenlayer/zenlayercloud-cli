name: DeregisterBackend
product: bmc
use: deregister-backend
short: 删除负载均衡后端配置服务器
long: 删除负载均衡后端配置服务器。
examples:
- cmd: zeno bmc deregister-backend --backend-id xxxx
  desc: 删除Backend
sdk:
  service: bmc
  version: '2026-02-01'
  action: DeregisterBackend
parameters:
- name: backend-id
  type: string
  required: true
  sdk-field: backendId
  description: 后端配置服务器的ID。
