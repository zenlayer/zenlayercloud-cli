name: AssociateEipAddress
product: bmc
use: associate-eip-address
short: 绑定Elastic IP
long: 将EIP绑定到同区域的机器实例上。
examples:
- cmd: zeno bmc associate-eip-address --eip-id eipId1 --instance-id instanceId1
  desc: EIP绑定机器实例
sdk:
  service: bmc
  version: '2026-02-01'
  action: AssociateEipAddress
parameters:
- name: eip-id
  type: string
  required: true
  sdk-field: eipId
  description: |-
    一个EIP ID。
    可通过DescribeEipAddresses接口返回值中的eipId获取。
- name: instance-id
  type: string
  required: true
  sdk-field: instanceId
  description: |-
    机器实例ID。
    可通过DescribeInstances接口返回值中的instanceId获取。
