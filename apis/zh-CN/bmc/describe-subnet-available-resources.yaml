name: DescribeSubnetAvailableResources
product: bmc
use: describe-subnet-available-resources
short: 查询支持创建Subnet的可用区
long: 本接口用于查询可创建Subnet资源的可用区。
sdk:
  service: bmc
  version: '2026-02-01'
  action: DescribeSubnetAvailableResources
parameters:
- name: zone-id
  type: string
  required: false
  sdk-field: zoneId
  description: |-
    可用区ID。
    不传则查询所有可创建Subnet的可用区。
