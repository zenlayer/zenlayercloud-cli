name: DeleteMember
product: user
use: delete-member
short: 删除成员
long: 删除一个成员信息。
examples:
- cmd: zeno user delete-member --user-uid userUid
  desc: 删除一个成员。
sdk:
  service: user
  version: '2024-05-29'
  action: DeleteMember
parameters:
- name: user-uid
  type: string
  required: true
  sdk-field: userUid
  description: 用户唯一ID。
