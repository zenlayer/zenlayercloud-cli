name: DeleteVpc
product: zec
use: delete-vpc
short: 删除VPC
long: 删除VPC
examples:
- cmd: zeno zec delete-vpc --vpc-id <vpcId>
  desc: 删除VPC。
sdk:
  service: zec
  version: '2025-09-01'
  action: DeleteVpc
parameters:
- name: vpc-id
  type: string
  required: true
  sdk-field: vpcId
  description: 要删除的VPC ID。
